server:
  port: 2000
spring:
  application:
    name: AIGOU-ZUUL
zuul:
  ignoredServices: '*'
  routes:
    plat:
      path: /plat/** #平台服务的请求路径
      serviceId: AIGOU-PLAT
  prefix: /services #请求前缀
